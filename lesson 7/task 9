На вход программе подается натуральное число n, которое является количеством чисел в последовательности, которую пользователь должен будет ввести.
Каждое число в последовательности вводится с новой строки.
Выведите YES, если среди введенных чисел было хотя бы одно нечетное число. В противном случае выведите NO.


number = int(input())
# сравнивать в программе между собой
# flag_max = flag_min = int(input())  # первое число последовательности для проверки
for i in range(number-1):  # работа цикла (-1 так как объява)
    number_next = int(input())
    if number % 2 == 0 or number_next % 2 == 0:  # проверка четности
        even = 'Yes'
if even != 'Yes':
    even = 'No'
print(even)
